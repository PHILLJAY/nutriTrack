[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "/docker-entrypoint.sh"
healthcheckPath = "/"
healthcheckTimeout = 300

[[mounts]]
mountPath = "/data"
volumeName = "nutritrack-data"
